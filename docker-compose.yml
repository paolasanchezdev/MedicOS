services:

  db:
    image: postgres:15-alpine
    container_name: medicos_db
    restart: always

    environment:
      POSTGRES_USER: medicos_app
      POSTGRES_PASSWORD: M3d1c0S_DB#2026!Secure
      POSTGRES_DB: medicos_db

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: